# Leave empty in local development to use the Vite same-origin proxy.
VITE_API_BASE_URL=

# Local Spring Boot address used only by the Vite development proxy.
VITE_API_PROXY_TARGET=http://localhost:8080
