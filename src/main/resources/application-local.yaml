spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/fintwin?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul}
    username: ${DB_USERNAME:fintwin}
    password: ${DB_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: update
